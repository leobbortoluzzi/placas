{
  "name": "placas-tag-redirect",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker: redirect NFC/QR tags via Supabase public.tag",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "check": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260811.1",
    "typescript": "^5.9.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.28.0"
  }
}
